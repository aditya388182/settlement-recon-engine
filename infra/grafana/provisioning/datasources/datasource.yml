apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: recon-prom            # every panel references this uid explicitly
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
